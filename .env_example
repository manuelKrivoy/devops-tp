# Puerto del servidor
PORT=3000

# Secreto para firmar tokens JWT (generá uno seguro en producción)
# Ejemplo: openssl rand -hex 32
JWT_SECRET=tu_secreto_super_seguro_aqui

# Tiempo de expiración del token
JWT_EXPIRES_IN=1h
